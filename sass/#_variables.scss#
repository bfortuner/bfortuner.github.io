/* Colors */
$main-color: red;
$background-color: white;

$white: #fff;
$light-gray: #dfdfdf;
$light-light-gray: #f8f8f8;
$gray: #b0b0b0;
$black: #333;



/* Typography */
$body-font: 'bookletter';
$body-font-italic: 'bookletter';
$body-font-size: 1.1em;

$blog-title-font: 'bookletter';
$blog-title-font-size: 2.3em;
$blog-title-text-transform: uppercase;
$blog-title-weight: bold;

$navi-font: 'bookletter';
$navi-font-size: 1.25em;

$page-title-align: center;

$post-font-size: 1.1em;
$post-font-line-height: 1.7em;
$post-align: justify;

$post-first-words-font-variant: small-caps;
$post-first-words-font-size: 1em;

$post-title-font-size: 1.6em;
$post-title-font-weight: normal;
$post-title-font-style: normal;
$post-title-line-height: 1.3em;
$post-title-align: center;

$date-font-size: .08em;
$date-font-style: normal;

$heading-font-size: 1.4em;
$heading-font-weight: normal;
$heading-font-style: normal;
$heading-top-spacing: 2em;

$subheading-font-size: 1.1em;
$subheading-font-weight: bold;
$subheading-font-style: normal;
$subheading-top-spacing: 2em;

$subheadings-align: left;

$code-font-family: 'Cousine';
$code-font-size: 0.75em;
$code-padding: 0.4em;
$code-margin: 0.4em;
$code-border-radius: 3px;
$code-border-color: $gray;
$code-background-color: $white;

$code-block-line-height: 1.8em;

$caption-font-size: 0.9em;
$caption-font-family: 'sorts-mill-goudy-italic';

$paginator-font-family: 'sorts-mill-goudy';
$paginator-font-size: 1em;

$blockquote-font-family: 'sorts-mill-goudy-italic';
$blockquote-font-size: 1em;

/* On the category listing page */
$year-font-family: 'nevis';
$year-font-size: 1.3em;
$year-spacing-bottom: 1.5em;
$month-font-family: 'nevis';
$month-font-size: 1em;
$month-spacing-bottom: 0.5em;
$month-indent: 2em;

/* Layout */

/* Width of the content area */
$content-width: 45%;

/* Space above and below the header section */
$header-spacing: 60px; 

/* Weight of the line at the top of the page */
$top-line-weight: 8px;     

/* Space between header links (this will be in ems) */
$navi-link-spacing: 1.2;     
$navi-top-spacing: 0.5em;

/* Space between the date and the post */
$post-content-top-space: 2em;

/* Space between the post and the author/tags */
$post-content-bottom-space: 1em; 

/* Space between posts in ems */
$space-between-posts: 2;

/* Author tags top and bottom spacing */
$tags-spacing: 1em;

/* Space before and after a <hr> */
$hr-spacing: 4em;

